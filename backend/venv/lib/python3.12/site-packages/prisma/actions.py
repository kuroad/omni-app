# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off
from __future__ import annotations

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template actions.py.jinja --
from typing import TypeVar
import warnings

from . import types, errors, bases
from ._compat import model_parse
from ._constants import CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED

if TYPE_CHECKING:
    from .client import Prisma
    from .bases import _PrismaModel


_PrismaModelT = TypeVar('_PrismaModelT', bound='_PrismaModel')


class CharacterActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Character]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await Character.prisma().query_raw(
            'SELECT * FROM Character WHERE id = $1',
            'bbadfchfja',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Character
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await Character.prisma().query_first(
            'SELECT * FROM Character WHERE name = $1',
            'dhheabfhf',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.CharacterCreateInput,
        include: Optional[types.CharacterInclude] = None
    ) -> _PrismaModelT:
        """Create a new Character record.

        Parameters
        ----------
        data
            Character record data
        include
            Specifies which relations should be loaded on the returned Character model

        Returns
        -------
        prisma.models.Character
            The created Character record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Character record from just the required fields
        character = await Character.prisma().create(
            data={
                # data to create a Character record
                'id': 'ggciceaie',
                'name': 'bbehjachib',
                'rarity': 2035015474,
                'path': 'dgiiaaijj',
                'element': 'bfaiacjjfc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.CharacterCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Character records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Character record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Character.prisma().create_many(
            data=[
                {
                    # data to create a Character record
                    'id': 'eigcfgbif',
                    'name': 'bagcfbhiig',
                    'rarity': 267834847,
                    'path': 'biabhbdai',
                    'element': 'idghgaicb',
                },
                {
                    # data to create a Character record
                    'id': 'fjfddhigg',
                    'name': 'hjaecfifb',
                    'rarity': 2111915288,
                    'path': 'bbejhfidcb',
                    'element': 'bgeecijdgg',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.CharacterWhereUniqueInput,
        include: Optional[types.CharacterInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Character record.

        Parameters
        ----------
        where
            Character filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Character model

        Returns
        -------
        prisma.models.Character
            The deleted Character record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        character = await Character.prisma().delete(
            where={
                'id': 'bdiicjafbj',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.CharacterWhereUniqueInput,
        include: Optional[types.CharacterInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Character record.

        Parameters
        ----------
        where
            Character filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Character model

        Returns
        -------
        prisma.models.Character
            The found Character record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        character = await Character.prisma().find_unique(
            where={
                'id': 'bgehebiafc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.CharacterWhereUniqueInput,
        include: Optional[types.CharacterInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Character record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Character filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Character model

        Returns
        -------
        prisma.models.Character
            The found Character record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        character = await Character.prisma().find_unique_or_raise(
            where={
                'id': 'bghffegacj',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.CharacterWhereInput] = None,
        cursor: Optional[types.CharacterWhereUniqueInput] = None,
        include: Optional[types.CharacterInclude] = None,
        order: Optional[Union[types.CharacterOrderByInput, List[types.CharacterOrderByInput]]] = None,
        distinct: Optional[List[types.CharacterScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Character records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Character records returned
        skip
            Ignore the first N results
        where
            Character filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Character model
        order
            Order the returned Character records by any field
        distinct
            Filter Character records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Character]
            The list of all Character records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Character records
        characters = await Character.prisma().find_many(take=10)

        # find the first 5 Character records ordered by the rarity field
        characters = await Character.prisma().find_many(
            take=5,
            order={
                'rarity': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.CharacterWhereInput] = None,
        cursor: Optional[types.CharacterWhereUniqueInput] = None,
        include: Optional[types.CharacterInclude] = None,
        order: Optional[Union[types.CharacterOrderByInput, List[types.CharacterOrderByInput]]] = None,
        distinct: Optional[List[types.CharacterScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Character record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Character filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Character model
        order
            Order the returned Character records by any field
        distinct
            Filter Character records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Character
            The first Character record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Character record ordered by the path field
        character = await Character.prisma().find_first(
            skip=1,
            order={
                'path': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.CharacterWhereInput] = None,
        cursor: Optional[types.CharacterWhereUniqueInput] = None,
        include: Optional[types.CharacterInclude] = None,
        order: Optional[Union[types.CharacterOrderByInput, List[types.CharacterOrderByInput]]] = None,
        distinct: Optional[List[types.CharacterScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Character record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Character filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Character model
        order
            Order the returned Character records by any field
        distinct
            Filter Character records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Character
            The first Character record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Character record ordered by the element field
        character = await Character.prisma().find_first_or_raise(
            skip=1,
            order={
                'element': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.CharacterUpdateInput,
        where: types.CharacterWhereUniqueInput,
        include: Optional[types.CharacterInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Character record.

        Parameters
        ----------
        data
            Character record data specifying what to update
        where
            Character filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Character model

        Returns
        -------
        prisma.models.Character
            The updated Character record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        character = await Character.prisma().update(
            where={
                'id': 'bhghchehcc',
            },
            data={
                # data to update the Character record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.CharacterWhereUniqueInput,
        data: types.CharacterUpsertInput,
        include: Optional[types.CharacterInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Character filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Character model

        Returns
        -------
        prisma.models.Character
            The created or updated Character record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        character = await Character.prisma().upsert(
            where={
                'id': 'dcgchcbbf',
            },
            data={
                'create': {
                    'id': 'dcgchcbbf',
                    'name': 'hjaecfifb',
                    'rarity': 2111915288,
                    'path': 'bbejhfidcb',
                    'element': 'bgeecijdgg',
                },
                'update': {
                    'name': 'hjaecfifb',
                    'rarity': 2111915288,
                    'path': 'bbejhfidcb',
                    'element': 'bgeecijdgg',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.CharacterUpdateManyMutationInput,
        where: types.CharacterWhereInput,
    ) -> int:
        """Update multiple Character records

        Parameters
        ----------
        data
            Character data to update the selected Character records to
        where
            Filter to select the Character records to update

        Returns
        -------
        int
            The total number of Character records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Character records
        total = await Character.prisma().update_many(
            data={
                'icon': 'bdedcabahc'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.CharacterWhereInput] = None,
        cursor: Optional[types.CharacterWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Character records present in the database

        Parameters
        ----------
        select
            Select the Character fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Character filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.CharacterCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Character.prisma().count()

        # results: prisma.types.CharacterCountAggregateOutput
        results = await Character.prisma().count(
            select={
                '_all': True,
                'preview': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.CharacterCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.CharacterWhereInput] = None,
        cursor: Optional[types.CharacterWhereUniqueInput] = None,
    ) -> types.CharacterCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.CharacterCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.CharacterWhereInput] = None,
        cursor: Optional[types.CharacterWhereUniqueInput] = None,
    ) -> Union[int, types.CharacterCountAggregateOutput]:
        """Count the number of Character records present in the database

        Parameters
        ----------
        select
            Select the Character fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Character filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.CharacterCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Character.prisma().count()

        # results: prisma.types.CharacterCountAggregateOutput
        results = await Character.prisma().count(
            select={
                '_all': True,
                'portrait': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.CharacterCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.CharacterWhereInput] = None
    ) -> int:
        """Delete multiple Character records.

        Parameters
        ----------
        where
            Optional Character filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Character records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Character records
        total = await Character.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.CharacterScalarFieldKeys'],
        *,
        where: Optional['types.CharacterWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.CharacterAvgAggregateInput'] = None,
        sum: Optional['types.CharacterSumAggregateInput'] = None,
        min: Optional['types.CharacterMinAggregateInput'] = None,
        max: Optional['types.CharacterMaxAggregateInput'] = None,
        having: Optional['types.CharacterScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.CharacterCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.CharacterScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.CharacterScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.CharacterGroupByOutput']:
        """Group Character records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Character fields to group records by
        where
            Character filter to select records
        take
            Limit the maximum number of Character records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.CharacterGroupByOutput]
            A list of dictionaries representing the Character record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Character records by updatedAt values
        # and count how many records are in each group
        results = await Character.prisma().group_by(
            ['updatedAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class RelicActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Relic]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await Relic.prisma().query_raw(
            'SELECT * FROM Relic WHERE id = $1',
            'ghfhiafcb',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Relic
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await Relic.prisma().query_first(
            'SELECT * FROM Relic WHERE name = $1',
            'heejgedji',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.RelicCreateInput,
        include: Optional[types.RelicInclude] = None
    ) -> _PrismaModelT:
        """Create a new Relic record.

        Parameters
        ----------
        data
            Relic record data
        include
            Specifies which relations should be loaded on the returned Relic model

        Returns
        -------
        prisma.models.Relic
            The created Relic record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Relic record from just the required fields
        relic = await Relic.prisma().create(
            data={
                # data to create a Relic record
                'id': 'bjgjgibgbf',
                'name': 'bbbgbhfjge',
                'rarity': 861472101,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.RelicCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Relic records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Relic record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Relic.prisma().create_many(
            data=[
                {
                    # data to create a Relic record
                    'id': 'bdadaadhag',
                    'name': 'bgiggdidbf',
                    'rarity': 2000430152,
                },
                {
                    # data to create a Relic record
                    'id': 'bigibebcib',
                    'name': 'bigaiehgcc',
                    'rarity': 1448521415,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.RelicWhereUniqueInput,
        include: Optional[types.RelicInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Relic record.

        Parameters
        ----------
        where
            Relic filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Relic model

        Returns
        -------
        prisma.models.Relic
            The deleted Relic record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        relic = await Relic.prisma().delete(
            where={
                'id': 'bgcigfahea',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.RelicWhereUniqueInput,
        include: Optional[types.RelicInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Relic record.

        Parameters
        ----------
        where
            Relic filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Relic model

        Returns
        -------
        prisma.models.Relic
            The found Relic record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        relic = await Relic.prisma().find_unique(
            where={
                'id': 'bcejgaggif',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.RelicWhereUniqueInput,
        include: Optional[types.RelicInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Relic record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Relic filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Relic model

        Returns
        -------
        prisma.models.Relic
            The found Relic record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        relic = await Relic.prisma().find_unique_or_raise(
            where={
                'id': 'idfjadbcc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.RelicWhereInput] = None,
        cursor: Optional[types.RelicWhereUniqueInput] = None,
        include: Optional[types.RelicInclude] = None,
        order: Optional[Union[types.RelicOrderByInput, List[types.RelicOrderByInput]]] = None,
        distinct: Optional[List[types.RelicScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Relic records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Relic records returned
        skip
            Ignore the first N results
        where
            Relic filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Relic model
        order
            Order the returned Relic records by any field
        distinct
            Filter Relic records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Relic]
            The list of all Relic records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Relic records
        relics = await Relic.prisma().find_many(take=10)

        # find the first 5 Relic records ordered by the setId field
        relics = await Relic.prisma().find_many(
            take=5,
            order={
                'setId': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.RelicWhereInput] = None,
        cursor: Optional[types.RelicWhereUniqueInput] = None,
        include: Optional[types.RelicInclude] = None,
        order: Optional[Union[types.RelicOrderByInput, List[types.RelicOrderByInput]]] = None,
        distinct: Optional[List[types.RelicScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Relic record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Relic filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Relic model
        order
            Order the returned Relic records by any field
        distinct
            Filter Relic records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Relic
            The first Relic record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Relic record ordered by the setName field
        relic = await Relic.prisma().find_first(
            skip=1,
            order={
                'setName': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.RelicWhereInput] = None,
        cursor: Optional[types.RelicWhereUniqueInput] = None,
        include: Optional[types.RelicInclude] = None,
        order: Optional[Union[types.RelicOrderByInput, List[types.RelicOrderByInput]]] = None,
        distinct: Optional[List[types.RelicScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Relic record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Relic filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Relic model
        order
            Order the returned Relic records by any field
        distinct
            Filter Relic records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Relic
            The first Relic record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Relic record ordered by the rarity field
        relic = await Relic.prisma().find_first_or_raise(
            skip=1,
            order={
                'rarity': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.RelicUpdateInput,
        where: types.RelicWhereUniqueInput,
        include: Optional[types.RelicInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Relic record.

        Parameters
        ----------
        data
            Relic record data specifying what to update
        where
            Relic filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Relic model

        Returns
        -------
        prisma.models.Relic
            The updated Relic record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        relic = await Relic.prisma().update(
            where={
                'id': 'hgdhbjhhj',
            },
            data={
                # data to update the Relic record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.RelicWhereUniqueInput,
        data: types.RelicUpsertInput,
        include: Optional[types.RelicInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Relic filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Relic model

        Returns
        -------
        prisma.models.Relic
            The created or updated Relic record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        relic = await Relic.prisma().upsert(
            where={
                'id': 'ecjjjfbae',
            },
            data={
                'create': {
                    'id': 'ecjjjfbae',
                    'name': 'bigaiehgcc',
                    'rarity': 1448521415,
                },
                'update': {
                    'name': 'bigaiehgcc',
                    'rarity': 1448521415,
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.RelicUpdateManyMutationInput,
        where: types.RelicWhereInput,
    ) -> int:
        """Update multiple Relic records

        Parameters
        ----------
        data
            Relic data to update the selected Relic records to
        where
            Filter to select the Relic records to update

        Returns
        -------
        int
            The total number of Relic records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Relic records
        total = await Relic.prisma().update_many(
            data={
                'icon': 'bhhfibbigf'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.RelicWhereInput] = None,
        cursor: Optional[types.RelicWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Relic records present in the database

        Parameters
        ----------
        select
            Select the Relic fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Relic filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.RelicCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Relic.prisma().count()

        # results: prisma.types.RelicCountAggregateOutput
        results = await Relic.prisma().count(
            select={
                '_all': True,
                'updatedAt': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.RelicCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.RelicWhereInput] = None,
        cursor: Optional[types.RelicWhereUniqueInput] = None,
    ) -> types.RelicCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.RelicCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.RelicWhereInput] = None,
        cursor: Optional[types.RelicWhereUniqueInput] = None,
    ) -> Union[int, types.RelicCountAggregateOutput]:
        """Count the number of Relic records present in the database

        Parameters
        ----------
        select
            Select the Relic fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Relic filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.RelicCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Relic.prisma().count()

        # results: prisma.types.RelicCountAggregateOutput
        results = await Relic.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.RelicCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.RelicWhereInput] = None
    ) -> int:
        """Delete multiple Relic records.

        Parameters
        ----------
        where
            Optional Relic filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Relic records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Relic records
        total = await Relic.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.RelicScalarFieldKeys'],
        *,
        where: Optional['types.RelicWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.RelicAvgAggregateInput'] = None,
        sum: Optional['types.RelicSumAggregateInput'] = None,
        min: Optional['types.RelicMinAggregateInput'] = None,
        max: Optional['types.RelicMaxAggregateInput'] = None,
        having: Optional['types.RelicScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.RelicCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.RelicScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.RelicScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.RelicGroupByOutput']:
        """Group Relic records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Relic fields to group records by
        where
            Relic filter to select records
        take
            Limit the maximum number of Relic records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.RelicGroupByOutput]
            A list of dictionaries representing the Relic record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Relic records by name values
        # and count how many records are in each group
        results = await Relic.prisma().group_by(
            ['name'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class LightConeActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.LightCone]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await LightCone.prisma().query_raw(
            'SELECT * FROM LightCone WHERE id = $1',
            'ijdbeffgg',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.LightCone
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await LightCone.prisma().query_first(
            'SELECT * FROM LightCone WHERE name = $1',
            'jjfeafhfj',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.LightConeCreateInput,
        include: Optional[types.LightConeInclude] = None
    ) -> _PrismaModelT:
        """Create a new LightCone record.

        Parameters
        ----------
        data
            LightCone record data
        include
            Specifies which relations should be loaded on the returned LightCone model

        Returns
        -------
        prisma.models.LightCone
            The created LightCone record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a LightCone record from just the required fields
        lightcone = await LightCone.prisma().create(
            data={
                # data to create a LightCone record
                'id': 'cbachdgfce',
                'name': 'chbfcacbd',
                'rarity': 456633834,
                'path': 'caficfigfb',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.LightConeCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple LightCone records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of LightCone record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await LightCone.prisma().create_many(
            data=[
                {
                    # data to create a LightCone record
                    'id': 'bfidgijfjc',
                    'name': 'ihieecagf',
                    'rarity': 1675280054,
                    'path': 'bgchfhgceh',
                },
                {
                    # data to create a LightCone record
                    'id': 'cafeiaccbc',
                    'name': 'gaddfhfh',
                    'rarity': 684462146,
                    'path': 'bgcffadich',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.LightConeWhereUniqueInput,
        include: Optional[types.LightConeInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single LightCone record.

        Parameters
        ----------
        where
            LightCone filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned LightCone model

        Returns
        -------
        prisma.models.LightCone
            The deleted LightCone record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        lightcone = await LightCone.prisma().delete(
            where={
                'id': 'fcbichhci',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.LightConeWhereUniqueInput,
        include: Optional[types.LightConeInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique LightCone record.

        Parameters
        ----------
        where
            LightCone filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned LightCone model

        Returns
        -------
        prisma.models.LightCone
            The found LightCone record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        lightcone = await LightCone.prisma().find_unique(
            where={
                'id': 'bcggadccgf',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.LightConeWhereUniqueInput,
        include: Optional[types.LightConeInclude] = None
    ) -> _PrismaModelT:
        """Find a unique LightCone record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            LightCone filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned LightCone model

        Returns
        -------
        prisma.models.LightCone
            The found LightCone record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        lightcone = await LightCone.prisma().find_unique_or_raise(
            where={
                'id': 'jdcfdcgc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.LightConeWhereInput] = None,
        cursor: Optional[types.LightConeWhereUniqueInput] = None,
        include: Optional[types.LightConeInclude] = None,
        order: Optional[Union[types.LightConeOrderByInput, List[types.LightConeOrderByInput]]] = None,
        distinct: Optional[List[types.LightConeScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple LightCone records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of LightCone records returned
        skip
            Ignore the first N results
        where
            LightCone filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned LightCone model
        order
            Order the returned LightCone records by any field
        distinct
            Filter LightCone records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.LightCone]
            The list of all LightCone records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 LightCone records
        lightcones = await LightCone.prisma().find_many(take=10)

        # find the first 5 LightCone records ordered by the rarity field
        lightcones = await LightCone.prisma().find_many(
            take=5,
            order={
                'rarity': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.LightConeWhereInput] = None,
        cursor: Optional[types.LightConeWhereUniqueInput] = None,
        include: Optional[types.LightConeInclude] = None,
        order: Optional[Union[types.LightConeOrderByInput, List[types.LightConeOrderByInput]]] = None,
        distinct: Optional[List[types.LightConeScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single LightCone record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            LightCone filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned LightCone model
        order
            Order the returned LightCone records by any field
        distinct
            Filter LightCone records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.LightCone
            The first LightCone record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second LightCone record ordered by the path field
        lightcone = await LightCone.prisma().find_first(
            skip=1,
            order={
                'path': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.LightConeWhereInput] = None,
        cursor: Optional[types.LightConeWhereUniqueInput] = None,
        include: Optional[types.LightConeInclude] = None,
        order: Optional[Union[types.LightConeOrderByInput, List[types.LightConeOrderByInput]]] = None,
        distinct: Optional[List[types.LightConeScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single LightCone record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            LightCone filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned LightCone model
        order
            Order the returned LightCone records by any field
        distinct
            Filter LightCone records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.LightCone
            The first LightCone record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second LightCone record ordered by the desc field
        lightcone = await LightCone.prisma().find_first_or_raise(
            skip=1,
            order={
                'desc': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.LightConeUpdateInput,
        where: types.LightConeWhereUniqueInput,
        include: Optional[types.LightConeInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single LightCone record.

        Parameters
        ----------
        data
            LightCone record data specifying what to update
        where
            LightCone filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned LightCone model

        Returns
        -------
        prisma.models.LightCone
            The updated LightCone record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        lightcone = await LightCone.prisma().update(
            where={
                'id': 'cafdaehjid',
            },
            data={
                # data to update the LightCone record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.LightConeWhereUniqueInput,
        data: types.LightConeUpsertInput,
        include: Optional[types.LightConeInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            LightCone filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned LightCone model

        Returns
        -------
        prisma.models.LightCone
            The created or updated LightCone record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        lightcone = await LightCone.prisma().upsert(
            where={
                'id': 'gifdddbia',
            },
            data={
                'create': {
                    'id': 'gifdddbia',
                    'name': 'gaddfhfh',
                    'rarity': 684462146,
                    'path': 'bgcffadich',
                },
                'update': {
                    'name': 'gaddfhfh',
                    'rarity': 684462146,
                    'path': 'bgcffadich',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.LightConeUpdateManyMutationInput,
        where: types.LightConeWhereInput,
    ) -> int:
        """Update multiple LightCone records

        Parameters
        ----------
        data
            LightCone data to update the selected LightCone records to
        where
            Filter to select the LightCone records to update

        Returns
        -------
        int
            The total number of LightCone records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all LightCone records
        total = await LightCone.prisma().update_many(
            data={
                'icon': 'bchehecef'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.LightConeWhereInput] = None,
        cursor: Optional[types.LightConeWhereUniqueInput] = None,
    ) -> int:
        """Count the number of LightCone records present in the database

        Parameters
        ----------
        select
            Select the LightCone fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            LightCone filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.LightConeCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await LightCone.prisma().count()

        # results: prisma.types.LightConeCountAggregateOutput
        results = await LightCone.prisma().count(
            select={
                '_all': True,
                'preview': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.LightConeCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.LightConeWhereInput] = None,
        cursor: Optional[types.LightConeWhereUniqueInput] = None,
    ) -> types.LightConeCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.LightConeCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.LightConeWhereInput] = None,
        cursor: Optional[types.LightConeWhereUniqueInput] = None,
    ) -> Union[int, types.LightConeCountAggregateOutput]:
        """Count the number of LightCone records present in the database

        Parameters
        ----------
        select
            Select the LightCone fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            LightCone filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.LightConeCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await LightCone.prisma().count()

        # results: prisma.types.LightConeCountAggregateOutput
        results = await LightCone.prisma().count(
            select={
                '_all': True,
                'portrait': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.LightConeCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.LightConeWhereInput] = None
    ) -> int:
        """Delete multiple LightCone records.

        Parameters
        ----------
        where
            Optional LightCone filter to find the records to be deleted

        Returns
        -------
        int
            The total number of LightCone records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all LightCone records
        total = await LightCone.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.LightConeScalarFieldKeys'],
        *,
        where: Optional['types.LightConeWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.LightConeAvgAggregateInput'] = None,
        sum: Optional['types.LightConeSumAggregateInput'] = None,
        min: Optional['types.LightConeMinAggregateInput'] = None,
        max: Optional['types.LightConeMaxAggregateInput'] = None,
        having: Optional['types.LightConeScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.LightConeCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.LightConeScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.LightConeScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.LightConeGroupByOutput']:
        """Group LightCone records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar LightCone fields to group records by
        where
            LightCone filter to select records
        take
            Limit the maximum number of LightCone records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.LightConeGroupByOutput]
            A list of dictionaries representing the LightCone record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group LightCone records by updatedAt values
        # and count how many records are in each group
        results = await LightCone.prisma().group_by(
            ['updatedAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class ItemActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Item]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await Item.prisma().query_raw(
            'SELECT * FROM Item WHERE id = $1',
            'jeijcbhfe',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Item
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await Item.prisma().query_first(
            'SELECT * FROM Item WHERE name = $1',
            'bjgejjabff',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.ItemCreateInput,
        include: Optional[types.ItemInclude] = None
    ) -> _PrismaModelT:
        """Create a new Item record.

        Parameters
        ----------
        data
            Item record data
        include
            Specifies which relations should be loaded on the returned Item model

        Returns
        -------
        prisma.models.Item
            The created Item record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Item record from just the required fields
        item = await Item.prisma().create(
            data={
                # data to create a Item record
                'id': 'bcciijbibg',
                'name': 'cffcachfd',
                'rarity': 1223573862,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.ItemCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Item records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Item record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Item.prisma().create_many(
            data=[
                {
                    # data to create a Item record
                    'id': 'febcgjbfj',
                    'name': 'bageiegghg',
                    'rarity': 508382461,
                },
                {
                    # data to create a Item record
                    'id': 'bacecgfhbe',
                    'name': 'ihcahiead',
                    'rarity': 1874748096,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.ItemWhereUniqueInput,
        include: Optional[types.ItemInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Item record.

        Parameters
        ----------
        where
            Item filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Item model

        Returns
        -------
        prisma.models.Item
            The deleted Item record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        item = await Item.prisma().delete(
            where={
                'id': 'jbgijghgb',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.ItemWhereUniqueInput,
        include: Optional[types.ItemInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Item record.

        Parameters
        ----------
        where
            Item filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Item model

        Returns
        -------
        prisma.models.Item
            The found Item record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        item = await Item.prisma().find_unique(
            where={
                'id': 'hgjcghfbi',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.ItemWhereUniqueInput,
        include: Optional[types.ItemInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Item record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Item filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Item model

        Returns
        -------
        prisma.models.Item
            The found Item record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        item = await Item.prisma().find_unique_or_raise(
            where={
                'id': 'icadbcehj',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ItemWhereInput] = None,
        cursor: Optional[types.ItemWhereUniqueInput] = None,
        include: Optional[types.ItemInclude] = None,
        order: Optional[Union[types.ItemOrderByInput, List[types.ItemOrderByInput]]] = None,
        distinct: Optional[List[types.ItemScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Item records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Item records returned
        skip
            Ignore the first N results
        where
            Item filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Item model
        order
            Order the returned Item records by any field
        distinct
            Filter Item records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Item]
            The list of all Item records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Item records
        items = await Item.prisma().find_many(take=10)

        # find the first 5 Item records ordered by the type field
        items = await Item.prisma().find_many(
            take=5,
            order={
                'type': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ItemWhereInput] = None,
        cursor: Optional[types.ItemWhereUniqueInput] = None,
        include: Optional[types.ItemInclude] = None,
        order: Optional[Union[types.ItemOrderByInput, List[types.ItemOrderByInput]]] = None,
        distinct: Optional[List[types.ItemScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Item record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Item filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Item model
        order
            Order the returned Item records by any field
        distinct
            Filter Item records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Item
            The first Item record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Item record ordered by the subType field
        item = await Item.prisma().find_first(
            skip=1,
            order={
                'subType': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ItemWhereInput] = None,
        cursor: Optional[types.ItemWhereUniqueInput] = None,
        include: Optional[types.ItemInclude] = None,
        order: Optional[Union[types.ItemOrderByInput, List[types.ItemOrderByInput]]] = None,
        distinct: Optional[List[types.ItemScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Item record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Item filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Item model
        order
            Order the returned Item records by any field
        distinct
            Filter Item records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Item
            The first Item record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Item record ordered by the rarity field
        item = await Item.prisma().find_first_or_raise(
            skip=1,
            order={
                'rarity': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.ItemUpdateInput,
        where: types.ItemWhereUniqueInput,
        include: Optional[types.ItemInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Item record.

        Parameters
        ----------
        data
            Item record data specifying what to update
        where
            Item filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Item model

        Returns
        -------
        prisma.models.Item
            The updated Item record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        item = await Item.prisma().update(
            where={
                'id': 'jchciaee',
            },
            data={
                # data to update the Item record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.ItemWhereUniqueInput,
        data: types.ItemUpsertInput,
        include: Optional[types.ItemInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Item filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Item model

        Returns
        -------
        prisma.models.Item
            The created or updated Item record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        item = await Item.prisma().upsert(
            where={
                'id': 'deeificjd',
            },
            data={
                'create': {
                    'id': 'deeificjd',
                    'name': 'ihcahiead',
                    'rarity': 1874748096,
                },
                'update': {
                    'name': 'ihcahiead',
                    'rarity': 1874748096,
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.ItemUpdateManyMutationInput,
        where: types.ItemWhereInput,
    ) -> int:
        """Update multiple Item records

        Parameters
        ----------
        data
            Item data to update the selected Item records to
        where
            Filter to select the Item records to update

        Returns
        -------
        int
            The total number of Item records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Item records
        total = await Item.prisma().update_many(
            data={
                'icon': 'bbcbhebbda'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ItemWhereInput] = None,
        cursor: Optional[types.ItemWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Item records present in the database

        Parameters
        ----------
        select
            Select the Item fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Item filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ItemCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Item.prisma().count()

        # results: prisma.types.ItemCountAggregateOutput
        results = await Item.prisma().count(
            select={
                '_all': True,
                'updatedAt': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.ItemCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ItemWhereInput] = None,
        cursor: Optional[types.ItemWhereUniqueInput] = None,
    ) -> types.ItemCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.ItemCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ItemWhereInput] = None,
        cursor: Optional[types.ItemWhereUniqueInput] = None,
    ) -> Union[int, types.ItemCountAggregateOutput]:
        """Count the number of Item records present in the database

        Parameters
        ----------
        select
            Select the Item fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Item filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ItemCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Item.prisma().count()

        # results: prisma.types.ItemCountAggregateOutput
        results = await Item.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ItemCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.ItemWhereInput] = None
    ) -> int:
        """Delete multiple Item records.

        Parameters
        ----------
        where
            Optional Item filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Item records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Item records
        total = await Item.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.ItemScalarFieldKeys'],
        *,
        where: Optional['types.ItemWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ItemAvgAggregateInput'] = None,
        sum: Optional['types.ItemSumAggregateInput'] = None,
        min: Optional['types.ItemMinAggregateInput'] = None,
        max: Optional['types.ItemMaxAggregateInput'] = None,
        having: Optional['types.ItemScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ItemCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ItemScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ItemScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ItemGroupByOutput']:
        """Group Item records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Item fields to group records by
        where
            Item filter to select records
        take
            Limit the maximum number of Item records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ItemGroupByOutput]
            A list of dictionaries representing the Item record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Item records by name values
        # and count how many records are in each group
        results = await Item.prisma().group_by(
            ['name'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class AchievementActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Achievement]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await Achievement.prisma().query_raw(
            'SELECT * FROM Achievement WHERE id = $1',
            'bejfijgcfb',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Achievement
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await Achievement.prisma().query_first(
            'SELECT * FROM Achievement WHERE seriesId = $1',
            'caifcbgii',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.AchievementCreateInput,
        include: Optional[types.AchievementInclude] = None
    ) -> _PrismaModelT:
        """Create a new Achievement record.

        Parameters
        ----------
        data
            Achievement record data
        include
            Specifies which relations should be loaded on the returned Achievement model

        Returns
        -------
        prisma.models.Achievement
            The created Achievement record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Achievement record from just the required fields
        achievement = await Achievement.prisma().create(
            data={
                # data to create a Achievement record
                'id': 'igaibbfgj',
                'title': 'bggajdcbbi',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.AchievementCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Achievement records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Achievement record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Achievement.prisma().create_many(
            data=[
                {
                    # data to create a Achievement record
                    'id': 'fcfhgbjed',
                    'title': 'hdgcajhjg',
                },
                {
                    # data to create a Achievement record
                    'id': 'ejdjahicb',
                    'title': 'gdjgigfgc',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.AchievementWhereUniqueInput,
        include: Optional[types.AchievementInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Achievement record.

        Parameters
        ----------
        where
            Achievement filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Achievement model

        Returns
        -------
        prisma.models.Achievement
            The deleted Achievement record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        achievement = await Achievement.prisma().delete(
            where={
                'id': 'gfeaahdeh',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.AchievementWhereUniqueInput,
        include: Optional[types.AchievementInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Achievement record.

        Parameters
        ----------
        where
            Achievement filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Achievement model

        Returns
        -------
        prisma.models.Achievement
            The found Achievement record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        achievement = await Achievement.prisma().find_unique(
            where={
                'id': 'bjafcgbffc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.AchievementWhereUniqueInput,
        include: Optional[types.AchievementInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Achievement record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Achievement filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Achievement model

        Returns
        -------
        prisma.models.Achievement
            The found Achievement record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        achievement = await Achievement.prisma().find_unique_or_raise(
            where={
                'id': 'hihegjif',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AchievementWhereInput] = None,
        cursor: Optional[types.AchievementWhereUniqueInput] = None,
        include: Optional[types.AchievementInclude] = None,
        order: Optional[Union[types.AchievementOrderByInput, List[types.AchievementOrderByInput]]] = None,
        distinct: Optional[List[types.AchievementScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Achievement records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Achievement records returned
        skip
            Ignore the first N results
        where
            Achievement filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Achievement model
        order
            Order the returned Achievement records by any field
        distinct
            Filter Achievement records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Achievement]
            The list of all Achievement records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Achievement records
        achievements = await Achievement.prisma().find_many(take=10)

        # find the first 5 Achievement records ordered by the title field
        achievements = await Achievement.prisma().find_many(
            take=5,
            order={
                'title': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.AchievementWhereInput] = None,
        cursor: Optional[types.AchievementWhereUniqueInput] = None,
        include: Optional[types.AchievementInclude] = None,
        order: Optional[Union[types.AchievementOrderByInput, List[types.AchievementOrderByInput]]] = None,
        distinct: Optional[List[types.AchievementScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Achievement record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Achievement filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Achievement model
        order
            Order the returned Achievement records by any field
        distinct
            Filter Achievement records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Achievement
            The first Achievement record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Achievement record ordered by the desc field
        achievement = await Achievement.prisma().find_first(
            skip=1,
            order={
                'desc': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.AchievementWhereInput] = None,
        cursor: Optional[types.AchievementWhereUniqueInput] = None,
        include: Optional[types.AchievementInclude] = None,
        order: Optional[Union[types.AchievementOrderByInput, List[types.AchievementOrderByInput]]] = None,
        distinct: Optional[List[types.AchievementScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Achievement record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Achievement filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Achievement model
        order
            Order the returned Achievement records by any field
        distinct
            Filter Achievement records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Achievement
            The first Achievement record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Achievement record ordered by the hidden field
        achievement = await Achievement.prisma().find_first_or_raise(
            skip=1,
            order={
                'hidden': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.AchievementUpdateInput,
        where: types.AchievementWhereUniqueInput,
        include: Optional[types.AchievementInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Achievement record.

        Parameters
        ----------
        data
            Achievement record data specifying what to update
        where
            Achievement filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Achievement model

        Returns
        -------
        prisma.models.Achievement
            The updated Achievement record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        achievement = await Achievement.prisma().update(
            where={
                'id': 'bdjidcidac',
            },
            data={
                # data to update the Achievement record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.AchievementWhereUniqueInput,
        data: types.AchievementUpsertInput,
        include: Optional[types.AchievementInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Achievement filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Achievement model

        Returns
        -------
        prisma.models.Achievement
            The created or updated Achievement record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        achievement = await Achievement.prisma().upsert(
            where={
                'id': 'ifgaaagff',
            },
            data={
                'create': {
                    'id': 'ifgaaagff',
                    'title': 'gdjgigfgc',
                },
                'update': {
                    'title': 'gdjgigfgc',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.AchievementUpdateManyMutationInput,
        where: types.AchievementWhereInput,
    ) -> int:
        """Update multiple Achievement records

        Parameters
        ----------
        data
            Achievement data to update the selected Achievement records to
        where
            Filter to select the Achievement records to update

        Returns
        -------
        int
            The total number of Achievement records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Achievement records
        total = await Achievement.prisma().update_many(
            data={
                'updatedAt': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AchievementWhereInput] = None,
        cursor: Optional[types.AchievementWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Achievement records present in the database

        Parameters
        ----------
        select
            Select the Achievement fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Achievement filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.AchievementCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Achievement.prisma().count()

        # results: prisma.types.AchievementCountAggregateOutput
        results = await Achievement.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.AchievementCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AchievementWhereInput] = None,
        cursor: Optional[types.AchievementWhereUniqueInput] = None,
    ) -> types.AchievementCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.AchievementCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AchievementWhereInput] = None,
        cursor: Optional[types.AchievementWhereUniqueInput] = None,
    ) -> Union[int, types.AchievementCountAggregateOutput]:
        """Count the number of Achievement records present in the database

        Parameters
        ----------
        select
            Select the Achievement fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Achievement filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.AchievementCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Achievement.prisma().count()

        # results: prisma.types.AchievementCountAggregateOutput
        results = await Achievement.prisma().count(
            select={
                '_all': True,
                'seriesId': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.AchievementCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.AchievementWhereInput] = None
    ) -> int:
        """Delete multiple Achievement records.

        Parameters
        ----------
        where
            Optional Achievement filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Achievement records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Achievement records
        total = await Achievement.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.AchievementScalarFieldKeys'],
        *,
        where: Optional['types.AchievementWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.AchievementAvgAggregateInput'] = None,
        sum: Optional['types.AchievementSumAggregateInput'] = None,
        min: Optional['types.AchievementMinAggregateInput'] = None,
        max: Optional['types.AchievementMaxAggregateInput'] = None,
        having: Optional['types.AchievementScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.AchievementCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.AchievementScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.AchievementScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.AchievementGroupByOutput']:
        """Group Achievement records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Achievement fields to group records by
        where
            Achievement filter to select records
        take
            Limit the maximum number of Achievement records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.AchievementGroupByOutput]
            A list of dictionaries representing the Achievement record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Achievement records by title values
        # and count how many records are in each group
        results = await Achievement.prisma().group_by(
            ['title'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class AvatarActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Avatar]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await Avatar.prisma().query_raw(
            'SELECT * FROM Avatar WHERE id = $1',
            'befcddgjce',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Avatar
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await Avatar.prisma().query_first(
            'SELECT * FROM Avatar WHERE name = $1',
            'bfhdbjjgfd',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.AvatarCreateInput,
        include: Optional[types.AvatarInclude] = None
    ) -> _PrismaModelT:
        """Create a new Avatar record.

        Parameters
        ----------
        data
            Avatar record data
        include
            Specifies which relations should be loaded on the returned Avatar model

        Returns
        -------
        prisma.models.Avatar
            The created Avatar record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Avatar record from just the required fields
        avatar = await Avatar.prisma().create(
            data={
                # data to create a Avatar record
                'id': 'cabdjadaji',
                'name': 'faajgfadf',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.AvatarCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Avatar records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Avatar record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Avatar.prisma().create_many(
            data=[
                {
                    # data to create a Avatar record
                    'id': 'biaagcedjc',
                    'name': 'cahhaghecf',
                },
                {
                    # data to create a Avatar record
                    'id': 'bghcbbcidi',
                    'name': 'jcgghhgdj',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.AvatarWhereUniqueInput,
        include: Optional[types.AvatarInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Avatar record.

        Parameters
        ----------
        where
            Avatar filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Avatar model

        Returns
        -------
        prisma.models.Avatar
            The deleted Avatar record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        avatar = await Avatar.prisma().delete(
            where={
                'id': 'beehgcebbg',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.AvatarWhereUniqueInput,
        include: Optional[types.AvatarInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Avatar record.

        Parameters
        ----------
        where
            Avatar filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Avatar model

        Returns
        -------
        prisma.models.Avatar
            The found Avatar record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        avatar = await Avatar.prisma().find_unique(
            where={
                'id': 'bhdiaidiaf',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.AvatarWhereUniqueInput,
        include: Optional[types.AvatarInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Avatar record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Avatar filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Avatar model

        Returns
        -------
        prisma.models.Avatar
            The found Avatar record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        avatar = await Avatar.prisma().find_unique_or_raise(
            where={
                'id': 'deajegcfi',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AvatarWhereInput] = None,
        cursor: Optional[types.AvatarWhereUniqueInput] = None,
        include: Optional[types.AvatarInclude] = None,
        order: Optional[Union[types.AvatarOrderByInput, List[types.AvatarOrderByInput]]] = None,
        distinct: Optional[List[types.AvatarScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Avatar records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Avatar records returned
        skip
            Ignore the first N results
        where
            Avatar filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Avatar model
        order
            Order the returned Avatar records by any field
        distinct
            Filter Avatar records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Avatar]
            The list of all Avatar records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Avatar records
        avatars = await Avatar.prisma().find_many(take=10)

        # find the first 5 Avatar records ordered by the icon field
        avatars = await Avatar.prisma().find_many(
            take=5,
            order={
                'icon': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.AvatarWhereInput] = None,
        cursor: Optional[types.AvatarWhereUniqueInput] = None,
        include: Optional[types.AvatarInclude] = None,
        order: Optional[Union[types.AvatarOrderByInput, List[types.AvatarOrderByInput]]] = None,
        distinct: Optional[List[types.AvatarScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Avatar record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Avatar filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Avatar model
        order
            Order the returned Avatar records by any field
        distinct
            Filter Avatar records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Avatar
            The first Avatar record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Avatar record ordered by the updatedAt field
        avatar = await Avatar.prisma().find_first(
            skip=1,
            order={
                'updatedAt': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.AvatarWhereInput] = None,
        cursor: Optional[types.AvatarWhereUniqueInput] = None,
        include: Optional[types.AvatarInclude] = None,
        order: Optional[Union[types.AvatarOrderByInput, List[types.AvatarOrderByInput]]] = None,
        distinct: Optional[List[types.AvatarScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Avatar record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Avatar filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Avatar model
        order
            Order the returned Avatar records by any field
        distinct
            Filter Avatar records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Avatar
            The first Avatar record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Avatar record ordered by the id field
        avatar = await Avatar.prisma().find_first_or_raise(
            skip=1,
            order={
                'id': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.AvatarUpdateInput,
        where: types.AvatarWhereUniqueInput,
        include: Optional[types.AvatarInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Avatar record.

        Parameters
        ----------
        data
            Avatar record data specifying what to update
        where
            Avatar filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Avatar model

        Returns
        -------
        prisma.models.Avatar
            The updated Avatar record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        avatar = await Avatar.prisma().update(
            where={
                'id': 'gabahhhjf',
            },
            data={
                # data to update the Avatar record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.AvatarWhereUniqueInput,
        data: types.AvatarUpsertInput,
        include: Optional[types.AvatarInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Avatar filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Avatar model

        Returns
        -------
        prisma.models.Avatar
            The created or updated Avatar record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        avatar = await Avatar.prisma().upsert(
            where={
                'id': 'cjagadcjg',
            },
            data={
                'create': {
                    'id': 'cjagadcjg',
                    'name': 'jcgghhgdj',
                },
                'update': {
                    'name': 'jcgghhgdj',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.AvatarUpdateManyMutationInput,
        where: types.AvatarWhereInput,
    ) -> int:
        """Update multiple Avatar records

        Parameters
        ----------
        data
            Avatar data to update the selected Avatar records to
        where
            Filter to select the Avatar records to update

        Returns
        -------
        int
            The total number of Avatar records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Avatar records
        total = await Avatar.prisma().update_many(
            data={
                'name': 'bifficggej'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AvatarWhereInput] = None,
        cursor: Optional[types.AvatarWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Avatar records present in the database

        Parameters
        ----------
        select
            Select the Avatar fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Avatar filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.AvatarCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Avatar.prisma().count()

        # results: prisma.types.AvatarCountAggregateOutput
        results = await Avatar.prisma().count(
            select={
                '_all': True,
                'icon': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.AvatarCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AvatarWhereInput] = None,
        cursor: Optional[types.AvatarWhereUniqueInput] = None,
    ) -> types.AvatarCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.AvatarCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AvatarWhereInput] = None,
        cursor: Optional[types.AvatarWhereUniqueInput] = None,
    ) -> Union[int, types.AvatarCountAggregateOutput]:
        """Count the number of Avatar records present in the database

        Parameters
        ----------
        select
            Select the Avatar fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Avatar filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.AvatarCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Avatar.prisma().count()

        # results: prisma.types.AvatarCountAggregateOutput
        results = await Avatar.prisma().count(
            select={
                '_all': True,
                'updatedAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.AvatarCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.AvatarWhereInput] = None
    ) -> int:
        """Delete multiple Avatar records.

        Parameters
        ----------
        where
            Optional Avatar filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Avatar records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Avatar records
        total = await Avatar.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.AvatarScalarFieldKeys'],
        *,
        where: Optional['types.AvatarWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.AvatarAvgAggregateInput'] = None,
        sum: Optional['types.AvatarSumAggregateInput'] = None,
        min: Optional['types.AvatarMinAggregateInput'] = None,
        max: Optional['types.AvatarMaxAggregateInput'] = None,
        having: Optional['types.AvatarScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.AvatarCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.AvatarScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.AvatarScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.AvatarGroupByOutput']:
        """Group Avatar records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Avatar fields to group records by
        where
            Avatar filter to select records
        take
            Limit the maximum number of Avatar records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.AvatarGroupByOutput]
            A list of dictionaries representing the Avatar record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Avatar records by id values
        # and count how many records are in each group
        results = await Avatar.prisma().group_by(
            ['id'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]



def _select_fields(root: str, select: Mapping[str, Any]) -> str:
    """Helper to build a GraphQL selection string

    This is a work around until field selection is added to the query builder.
    """

    return root + ' {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))


from . import models